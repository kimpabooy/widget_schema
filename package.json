{
  "devDependencies": {
    "playwright": "^1.57.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "helmet": "^8.1.0"
  },
  "scripts": {
    "build": "node index.js",
    "start": "npm run build && node server.js"
  }
}
